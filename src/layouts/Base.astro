---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  ogType?: 'website' | 'article';
  publishedTime?: Date;
  author?: string;
}

const {
  title,
  description,
  ogType = 'website',
  publishedTime,
  author = 'Akshan Krithick Ponvel',
} = Astro.props;

const siteTitle = 'Akshan';
const fullTitle = title ? `${title} — ${siteTitle}` : `${siteTitle} — Writing`;
const desc =
  description ??
  'Notes and essays on machine learning, data, and the field of artificial intelligence in general.';
const canonical = new URL(Astro.url.pathname, Astro.site).toString();

const personSchema = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Akshan Krithick Ponvel',
  alternateName: ['Akshan Krithick', 'Akshan'],
  url: 'https://akshan.dev',
  sameAs: ['https://github.com/akshan-main'],
};
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="generator" content={Astro.generator} />

  <title>{fullTitle}</title>
  <meta name="description" content={desc} />
  <meta name="author" content={author} />
  <link rel="canonical" href={canonical} />

  <meta property="og:type" content={ogType} />
  <meta property="og:site_name" content={siteTitle} />
  <meta property="og:title" content={fullTitle} />
  <meta property="og:description" content={desc} />
  <meta property="og:url" content={canonical} />
  {publishedTime && (
    <meta property="article:published_time" content={publishedTime.toISOString()} />
  )}
  {ogType === 'article' && <meta property="article:author" content={author} />}

  <meta name="twitter:card" content="summary" />
  <meta name="twitter:title" content={fullTitle} />
  <meta name="twitter:description" content={desc} />

  <link rel="alternate" type="application/rss+xml" title={`${siteTitle} — RSS`} href="/rss.xml" />
  <link rel="sitemap" type="application/xml" href="/sitemap-index.xml" />

  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect width='100' height='100' fill='white'/><text y='.82em' x='50%25' text-anchor='middle' font-size='90' fill='black' font-family='Helvetica, Arial, sans-serif' font-weight='400'>%E2%84%AE</text></svg>" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500;1,600&family=JetBrains+Mono:wght@400;500;600;700&family=Archivo+Black&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css" integrity="sha384-nB0miv6/jRmo5UMMR1wu3Gz6NLsoTkbqJghGIsx//Rlm+ZU03BU6SQNC66uf4l5+" crossorigin="anonymous" />

  <script is:inline>
    (() => {
      const root = document.documentElement;
      const theme = localStorage.getItem('theme') === 'grey' ? 'grey' : 'light';
      root.setAttribute('data-theme', theme);
      if (localStorage.getItem('show-mini') === 'true') {
        root.classList.add('show-mini');
      }
    })();
  </script>
</head>
<body>
  <nav>
    <a class="nav-left" href="/">akshan</a>
    <div class="nav-right">
      <a href="/">writing</a>
      <a href="/tags/">tags</a>
      <a href="mailto:akshankrithick305@gmail.com">email</a>
      <button
        class="theme-toggle"
        id="theme-toggle"
        type="button"
        role="switch"
        aria-checked="false"
        aria-label="Toggle color theme"
      >
        <span class="theme-toggle-thumb" aria-hidden="true"></span>
      </button>
    </div>
  </nav>
  <main>
    <slot />

    <footer class="colophon">
      <p class="col-note">
        This is my blog. I write about machine learning and data, and I try to
        only publish things you won't find anywhere else.
      </p>
      <div class="col-field">
        <span class="lbl">Author</span>
        <span class="val">Akshan Krithick</span>
      </div>
      <a class="col-link" href="https://github.com/akshan-main">GitHub -&gt;</a>
      <a class="col-link" href="/rss.xml">RSS -&gt;</a>
      <a class="col-link" href="mailto:akshankrithick305@gmail.com">Email -&gt;</a>
      <a class="col-link" href="/tags/">Tags -&gt;</a>
      <div class="col-end">© 2026 Akshan Krithick</div>
    </footer>
  </main>

  <a
    class="admin-link"
    href="/admin/#/collections/posts"
    target="_blank"
    rel="noopener"
    aria-hidden="true"
    tabindex="-1"
  >·</a>

  <script
    type="application/ld+json"
    set:html={JSON.stringify(personSchema)}
  />

  <script is:inline>
    (() => {
      const btn = document.getElementById('theme-toggle');
      if (!btn) return;
      const sync = (theme) => {
        btn.setAttribute('aria-checked', theme === 'grey' ? 'true' : 'false');
      };
      sync(document.documentElement.getAttribute('data-theme') || 'light');
      btn.addEventListener('click', () => {
        const current = document.documentElement.getAttribute('data-theme') || 'light';
        const next = current === 'grey' ? 'light' : 'grey';
        document.documentElement.setAttribute('data-theme', next);
        localStorage.setItem('theme', next);
        sync(next);
      });
    })();
  </script>
</body>
</html>
